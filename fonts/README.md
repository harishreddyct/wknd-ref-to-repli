# fonts/

Intentionally empty. The reference site's real webfont couldn't be confirmed (no computed-style or network access to the live site was available), so this project uses the system-ui stack defined in `styles/styles.css` instead of guessing a look-alike font. See `docs/component-registry.md`.
