// Purely presentational — CSS + semantic <ol> is enough, no decoration needed.
// The file still exists so the DA authoring round-trip recognizes "breadcrumbs"
// as a real block and preserves the class (see docs/component-registry.md).
export default function decorate() {}
